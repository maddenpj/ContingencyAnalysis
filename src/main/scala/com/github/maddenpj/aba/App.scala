package com.github.maddenpj.aba


object App {
  def test() = {
    // val aCoding = CodeSet.fromFile("antecedent", "sample/antecedent.json").get
    // val bCoding = CodeSet.fromFile("behavior", "sample/behavior.json").get

    // val abEvents = Event.fromTextFile("AB.txt").flatten.map { case (a, b, res) =>
    //   ABEvent(aCoding(a), bCoding(b), Response.withName(res))
    // }
  }
  def main(args: Array[String]) = {}
}
